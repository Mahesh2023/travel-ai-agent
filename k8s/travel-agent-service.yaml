apiVersion: v1
kind: Service
metadata:
  name: travel-agent
  namespace: travel-agent
  labels:
    app: travel-agent
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: travel-agent
