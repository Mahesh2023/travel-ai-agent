# Travel AI Agent - Scalable Dependencies (following teloscopy pattern)
# FastAPI + Vanilla HTML/CSS/JS + LLM Integration + Redis + PostgreSQL

# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic>=2.6.0
python-dotenv==1.0.0

# Redis for distributed rate limiting and session management
redis>=5.0.0

# PostgreSQL for persistence
asyncpg>=0.29.0
sqlalchemy>=2.0.0
alembic>=1.12.0

# LLM integration uses standard library (urllib) for OpenAI-compatible APIs
