# Travel AI Agent - Simplified Dependencies (Following Teloscopy Pattern)
# Single service, no external databases, in-memory storage only

# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic>=2.6.0
python-dotenv==1.0.0

# Authentication (JWT + bcrypt)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Voice/Speech recognition
SpeechRecognition==3.10.0
pydub==0.25.1

# Text-to-Speech
gTTS==2.4.0

# LLM integration uses standard library (urllib) for OpenAI-compatible APIs
